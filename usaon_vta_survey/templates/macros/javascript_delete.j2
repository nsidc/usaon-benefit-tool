{% macro add_javascript__delete() -%}
  <script>
    function remove_relationship(url, redirect_url) {
      fetch(url, { method: 'DELETE'});
      location.assign(redirect_url);
        }
  </script>
{% endmacro  %}
